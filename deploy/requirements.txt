# Minimal runtime deps for the policy-doctor demo app.
# Pinned conservatively; tighten further before deployment if needed.

streamlit>=1.33
numpy>=1.24,<3
plotly>=5.18
pyyaml>=6
networkx>=3
scipy>=1.10
pandas>=2

# matplotlib is eagerly imported by policy_doctor.plotting.__init__
# (via policy_comparison helpers used elsewhere in the package). We don't
# actually render any matplotlib figures in the demo, but the import has
# to resolve.
matplotlib>=3.7

# Pyvis: the legacy Markov-graph renderer for the main app's
# behavior_graph tab. Optional for the demo's primary flow but the same
# code paths import it.
pyvis>=0.3

# Video frames + thumbnails (used by the click-to-explore panel).
imageio>=2.31
imageio-ffmpeg>=0.4.9
pillow>=10

# GCS response storage (optional — only imported when SURVEY_GCS_BUCKET is set).
google-cloud-storage>=2.10
