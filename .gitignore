__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
*.egg-info/
.eggs/
dist/
build/
outputs/
.hydra/
.cache/

# uv-managed venvs (one per env: analysis, cupid, mimicgen, robocasa).
# Selected at runtime via UV_PROJECT_ENVIRONMENT; see scripts/uv_env.sh.
.venv/
.venvs/

# Local secrets and tooling state
.env
.claude/

# Local run-output dirs (treated as transient; not source)
/experiments/

# Pre-implementation spec, superseded by docs/experiments/experiment_e2_agent_proposals.md
/E2_vlm_proposals.md

# Clustering artifacts — derived from eval data via scripts/sweep_clusterings_for_demo.sh
# and bundled into the docker image via deploy/collect_artifacts.sh. Not source.
third_party/influence_visualizer/configs/*/clustering/

# Sweep outputs and eval/source data (symlinked to SSD).
/data/
/logs/

# Deploy bundle (regenerated by deploy/collect_artifacts.sh).
/deploy/policy_doctor/
/deploy/third_party/
/deploy/data/
