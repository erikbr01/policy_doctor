"""Policy Doctor — curation pipeline orchestration and influence tooling.

Policy **training** (diffusion, Hydra workspaces) is **not** shipped here; the curation
pipeline composes configs from paths under ``repo_root`` (typically ``third_party/cupid``).
"""

__version__ = "0.1.0"
